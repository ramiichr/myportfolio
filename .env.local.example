# This file is for local development with PostgreSQL
# Copy this to .env.local and update the DATABASE_URL

# PostgreSQL Database URL for local development
# Replace with your local PostgreSQL connection string
DATABASE_URL="postgresql://username:password@localhost:5432/myportfolio?schema=public"

# Optional: Redis for caching (local development)
# UPSTASH_REDIS_REST_URL="your-redis-url"
# UPSTASH_REDIS_REST_TOKEN="your-redis-token"

# Optional: Email configuration (if using Resend)
# RESEND_API_KEY="your-resend-api-key"

# Optional: GitHub token for contributions graph
# GITHUB_TOKEN="your-github-token"
