# Database
DATABASE_URL="postgresql://username:password@hostname:port/database?schema=public"

# For Vercel deployment, use Vercel Postgres or another cloud database
# DATABASE_URL will be automatically set by Vercel when using Vercel Postgres

# Optional: Redis for caching (if using Upstash Redis)
# UPSTASH_REDIS_REST_URL="your-redis-url"
# UPSTASH_REDIS_REST_TOKEN="your-redis-token"

# Email configuration (if using Resend)
# RESEND_API_KEY="your-resend-api-key"

# GitHub token for contributions graph (if needed)
# GITHUB_TOKEN="your-github-token"
